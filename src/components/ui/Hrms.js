import React from 'react';
import { Grid, Typography } from '@mui/material';

const Hrms = () => {
    return (
        <Grid sx={{color:"green"}}>
            <Typography variant="h1">HRMS</Typography>
            <Typography variant="h5">Your complete HRM Solution</Typography>
        </Grid>
    );
};

export default Hrms;
